# Copy this file to `.env` and fill in the values once.
# Docker Compose will auto-load `.env` from the project root.

GROQ_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
DATAGOV_API_KEY=
