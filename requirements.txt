# ── Document Extraction (needed immediately) ────────────────────────────────
rapidocr>=1.0.0           # OCR engine for scanned images and PDFs (replaces Tesseract)
numpy>=2.0.0              # required by RapidOCR for image array conversion
pdfplumber==0.11.4        # extracts text from digital PDFs
Pillow==10.4.0            # image handling
python-docx==1.1.2        # extracts text from Word (.docx) instructions file
docling>=2.0              # layout + markdown extraction (optional, heavier alternative)
pymupdf>=1.24             # PDF page rendering for scanned PDF fallback
onnxruntime>=1.20         # RapidOCR backend used by Docling OCR in Docker

# ── AI / Groq API ────────────────────────────────────────────────────────────
groq>=0.9.0               # Groq API client — fast LLM inference (LLaMA 3, etc.)

# ── API (for later — serving the model) ──────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# ── ML / Training (for later — fine-tuning DistilBERT) ───────────────────────
torch>=2.6.0            # 2.4.x does not support Python 3.13; 2.6+ does
transformers>=4.44.2
datasets>=3.0.1
scikit-learn>=1.5.2
numpy>=2.0.0            # numpy 1.26.x does not support Python 3.13; 2.0+ does
pandas>=2.2.3

# ── Evaluation / Visualisation (for later) ────────────────────────────────────
matplotlib==3.9.2
seaborn==0.13.2

boto3>=1.28.0              # AWS SDK for Python (for Bedrock API calls)